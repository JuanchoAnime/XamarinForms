<?xml version="1.0" encoding="utf-8" ?>
<rg:PopupPage
        xmlns:rg="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" 
        xmlns="http://xamarin.com/schemas/2014/forms"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:d="http://xamarin.com/schemas/2014/forms/design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        BindingContext="{Binding Main, Source={StaticResource InstanceLocator}}"
        x:Class="PruebasUi2.Component.MessageBox">


    <Frame
        HorizontalOptions="CenterAndExpand"
        VerticalOptions="CenterAndExpand"
        BindingContext="{Binding MessageBox}">
        <StackLayout Orientation="Horizontal">
            <ActivityIndicator IsVisible="True" IsRunning="True" Color="DarkBlue" Margin="5" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Mensaje}" FontSize="18" TextColor="DarkBlue" Padding="5" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand" />
            
        </StackLayout>
    </Frame>

</rg:PopupPage>